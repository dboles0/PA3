#include "test.h"
//#include "multi-lookup.h"

int main(int argc, char *argv[]){

	int queue_max = 10;
	struct fifo_Q * q = create_queue(queue_max);


	// read parameters form the command line(section 4.1) with names
	if(argc < MIN_ARGS)
	// names of files to be processed
	
	// create queue
	test_cases(q, queue_max);

	// simple application to create and wait for termination of threads

	// print total runtime of pressesses to standard output using gettimeofday()
	return 0;

			
} 
